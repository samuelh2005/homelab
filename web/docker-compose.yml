services:
  docuwiki:
    image: dokuwiki/dokuwiki:latest
    environment:
      - PHP_TIMEZONE=Europe/London
    volumes:
      - ./dokuwiki_data:/storage
    ports:
      - "80:80"
    restart: unless-stopped
